[build]
  publish = "."
  functions = "netlify/functions"

[[redirects]]
  from = "/api/send-result"
  to = "/.netlify/functions/send-result"
  status = 200

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
